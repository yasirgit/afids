<?php

/**
 * Base class that represents a row from the 'airport' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Tue May 24 06:33:24 2011
 *
 * @package    lib.model.om
 */
abstract class BaseAirport extends BaseObject  implements Persistent {


  const PEER = 'AirportPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        AirportPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the ident field.
	 * @var        string
	 */
	protected $ident;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the city field.
	 * @var        string
	 */
	protected $city;

	/**
	 * The value for the state field.
	 * @var        string
	 */
	protected $state;

	/**
	 * The value for the latitude field.
	 * Note: this column has a database default value of: 0
	 * @var        double
	 */
	protected $latitude;

	/**
	 * The value for the longitude field.
	 * Note: this column has a database default value of: 0
	 * @var        double
	 */
	protected $longitude;

	/**
	 * The value for the runway_length field.
	 * @var        int
	 */
	protected $runway_length;

	/**
	 * The value for the wing_id field.
	 * @var        int
	 */
	protected $wing_id;

	/**
	 * The value for the gmt_offset field.
	 * @var        int
	 */
	protected $gmt_offset;

	/**
	 * The value for the dst_offset field.
	 * @var        int
	 */
	protected $dst_offset;

	/**
	 * The value for the zipcode field.
	 * @var        string
	 */
	protected $zipcode;

	/**
	 * The value for the closed field.
	 * @var        int
	 */
	protected $closed;

	/**
	 * The value for the faa_site_number field.
	 * @var        string
	 */
	protected $faa_site_number;

	/**
	 * The value for the atct field.
	 * @var        int
	 */
	protected $atct;

	/**
	 * The value for the is_private field.
	 * @var        int
	 */
	protected $is_private;

	/**
	 * The value for the non_commercial_landing_fee field.
	 * @var        int
	 */
	protected $non_commercial_landing_fee;

	/**
	 * The value for the elevation field.
	 * @var        int
	 */
	protected $elevation;

	/**
	 * @var        Wing
	 */
	protected $aWing;

	/**
	 * @var        array Camp[] Collection to store aggregation of Camp objects.
	 */
	protected $collCamps;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCamps.
	 */
	private $lastCampCriteria = null;

	/**
	 * @var        array Fbo[] Collection to store aggregation of Fbo objects.
	 */
	protected $collFbos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFbos.
	 */
	private $lastFboCriteria = null;

	/**
	 * @var        array MissionLeg[] Collection to store aggregation of MissionLeg objects.
	 */
	protected $collMissionLegsRelatedByFromAirportId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMissionLegsRelatedByFromAirportId.
	 */
	private $lastMissionLegRelatedByFromAirportIdCriteria = null;

	/**
	 * @var        array MissionLeg[] Collection to store aggregation of MissionLeg objects.
	 */
	protected $collMissionLegsRelatedByToAirportId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMissionLegsRelatedByToAirportId.
	 */
	private $lastMissionLegRelatedByToAirportIdCriteria = null;

	/**
	 * @var        array Pilot[] Collection to store aggregation of Pilot objects.
	 */
	protected $collPilots;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPilots.
	 */
	private $lastPilotCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseAirport object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->latitude = 0;
		$this->longitude = 0;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [ident] column value.
	 * 
	 * @return     string
	 */
	public function getIdent()
	{
		return $this->ident;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [city] column value.
	 * 
	 * @return     string
	 */
	public function getCity()
	{
		return $this->city;
	}

	/**
	 * Get the [state] column value.
	 * 
	 * @return     string
	 */
	public function getState()
	{
		return $this->state;
	}

	/**
	 * Get the [latitude] column value.
	 * 
	 * @return     double
	 */
	public function getLatitude()
	{
		return $this->latitude;
	}

	/**
	 * Get the [longitude] column value.
	 * 
	 * @return     double
	 */
	public function getLongitude()
	{
		return $this->longitude;
	}

	/**
	 * Get the [runway_length] column value.
	 * 
	 * @return     int
	 */
	public function getRunwayLength()
	{
		return $this->runway_length;
	}

	/**
	 * Get the [wing_id] column value.
	 * 
	 * @return     int
	 */
	public function getWingId()
	{
		return $this->wing_id;
	}

	/**
	 * Get the [gmt_offset] column value.
	 * 
	 * @return     int
	 */
	public function getGmtOffset()
	{
		return $this->gmt_offset;
	}

	/**
	 * Get the [dst_offset] column value.
	 * 
	 * @return     int
	 */
	public function getDstOffset()
	{
		return $this->dst_offset;
	}

	/**
	 * Get the [zipcode] column value.
	 * 
	 * @return     string
	 */
	public function getZipcode()
	{
		return $this->zipcode;
	}

	/**
	 * Get the [closed] column value.
	 * 
	 * @return     int
	 */
	public function getClosed()
	{
		return $this->closed;
	}

	/**
	 * Get the [faa_site_number] column value.
	 * 
	 * @return     string
	 */
	public function getFaaSiteNumber()
	{
		return $this->faa_site_number;
	}

	/**
	 * Get the [atct] column value.
	 * 
	 * @return     int
	 */
	public function getAtct()
	{
		return $this->atct;
	}

	/**
	 * Get the [is_private] column value.
	 * 
	 * @return     int
	 */
	public function getIsPrivate()
	{
		return $this->is_private;
	}

	/**
	 * Get the [non_commercial_landing_fee] column value.
	 * 
	 * @return     int
	 */
	public function getNonCommercialLandingFee()
	{
		return $this->non_commercial_landing_fee;
	}

	/**
	 * Get the [elevation] column value.
	 * 
	 * @return     int
	 */
	public function getElevation()
	{
		return $this->elevation;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = AirportPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [ident] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setIdent($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ident !== $v) {
			$this->ident = $v;
			$this->modifiedColumns[] = AirportPeer::IDENT;
		}

		return $this;
	} // setIdent()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = AirportPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [city] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setCity($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->city !== $v) {
			$this->city = $v;
			$this->modifiedColumns[] = AirportPeer::CITY;
		}

		return $this;
	} // setCity()

	/**
	 * Set the value of [state] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setState($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->state !== $v) {
			$this->state = $v;
			$this->modifiedColumns[] = AirportPeer::STATE;
		}

		return $this;
	} // setState()

	/**
	 * Set the value of [latitude] column.
	 * 
	 * @param      double $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setLatitude($v)
	{
		if ($v !== null) {
			$v = (double) $v;
		}

		if ($this->latitude !== $v || $v === 0) {
			$this->latitude = $v;
			$this->modifiedColumns[] = AirportPeer::LATITUDE;
		}

		return $this;
	} // setLatitude()

	/**
	 * Set the value of [longitude] column.
	 * 
	 * @param      double $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setLongitude($v)
	{
		if ($v !== null) {
			$v = (double) $v;
		}

		if ($this->longitude !== $v || $v === 0) {
			$this->longitude = $v;
			$this->modifiedColumns[] = AirportPeer::LONGITUDE;
		}

		return $this;
	} // setLongitude()

	/**
	 * Set the value of [runway_length] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setRunwayLength($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->runway_length !== $v) {
			$this->runway_length = $v;
			$this->modifiedColumns[] = AirportPeer::RUNWAY_LENGTH;
		}

		return $this;
	} // setRunwayLength()

	/**
	 * Set the value of [wing_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setWingId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->wing_id !== $v) {
			$this->wing_id = $v;
			$this->modifiedColumns[] = AirportPeer::WING_ID;
		}

		if ($this->aWing !== null && $this->aWing->getId() !== $v) {
			$this->aWing = null;
		}

		return $this;
	} // setWingId()

	/**
	 * Set the value of [gmt_offset] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setGmtOffset($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->gmt_offset !== $v) {
			$this->gmt_offset = $v;
			$this->modifiedColumns[] = AirportPeer::GMT_OFFSET;
		}

		return $this;
	} // setGmtOffset()

	/**
	 * Set the value of [dst_offset] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setDstOffset($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dst_offset !== $v) {
			$this->dst_offset = $v;
			$this->modifiedColumns[] = AirportPeer::DST_OFFSET;
		}

		return $this;
	} // setDstOffset()

	/**
	 * Set the value of [zipcode] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setZipcode($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->zipcode !== $v) {
			$this->zipcode = $v;
			$this->modifiedColumns[] = AirportPeer::ZIPCODE;
		}

		return $this;
	} // setZipcode()

	/**
	 * Set the value of [closed] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setClosed($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->closed !== $v) {
			$this->closed = $v;
			$this->modifiedColumns[] = AirportPeer::CLOSED;
		}

		return $this;
	} // setClosed()

	/**
	 * Set the value of [faa_site_number] column.
	 * 
	 * @param      string $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setFaaSiteNumber($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->faa_site_number !== $v) {
			$this->faa_site_number = $v;
			$this->modifiedColumns[] = AirportPeer::FAA_SITE_NUMBER;
		}

		return $this;
	} // setFaaSiteNumber()

	/**
	 * Set the value of [atct] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setAtct($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->atct !== $v) {
			$this->atct = $v;
			$this->modifiedColumns[] = AirportPeer::ATCT;
		}

		return $this;
	} // setAtct()

	/**
	 * Set the value of [is_private] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setIsPrivate($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->is_private !== $v) {
			$this->is_private = $v;
			$this->modifiedColumns[] = AirportPeer::IS_PRIVATE;
		}

		return $this;
	} // setIsPrivate()

	/**
	 * Set the value of [non_commercial_landing_fee] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setNonCommercialLandingFee($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->non_commercial_landing_fee !== $v) {
			$this->non_commercial_landing_fee = $v;
			$this->modifiedColumns[] = AirportPeer::NON_COMMERCIAL_LANDING_FEE;
		}

		return $this;
	} // setNonCommercialLandingFee()

	/**
	 * Set the value of [elevation] column.
	 * 
	 * @param      int $v new value
	 * @return     Airport The current object (for fluent API support)
	 */
	public function setElevation($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->elevation !== $v) {
			$this->elevation = $v;
			$this->modifiedColumns[] = AirportPeer::ELEVATION;
		}

		return $this;
	} // setElevation()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(AirportPeer::LATITUDE,AirportPeer::LONGITUDE))) {
				return false;
			}

			if ($this->latitude !== 0) {
				return false;
			}

			if ($this->longitude !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->ident = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->name = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->city = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->state = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->latitude = ($row[$startcol + 5] !== null) ? (double) $row[$startcol + 5] : null;
			$this->longitude = ($row[$startcol + 6] !== null) ? (double) $row[$startcol + 6] : null;
			$this->runway_length = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->wing_id = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->gmt_offset = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->dst_offset = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->zipcode = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->closed = ($row[$startcol + 12] !== null) ? (int) $row[$startcol + 12] : null;
			$this->faa_site_number = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->atct = ($row[$startcol + 14] !== null) ? (int) $row[$startcol + 14] : null;
			$this->is_private = ($row[$startcol + 15] !== null) ? (int) $row[$startcol + 15] : null;
			$this->non_commercial_landing_fee = ($row[$startcol + 16] !== null) ? (int) $row[$startcol + 16] : null;
			$this->elevation = ($row[$startcol + 17] !== null) ? (int) $row[$startcol + 17] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 18; // 18 = AirportPeer::NUM_COLUMNS - AirportPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Airport object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aWing !== null && $this->wing_id !== $this->aWing->getId()) {
			$this->aWing = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AirportPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = AirportPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aWing = null;
			$this->collCamps = null;
			$this->lastCampCriteria = null;

			$this->collFbos = null;
			$this->lastFboCriteria = null;

			$this->collMissionLegsRelatedByFromAirportId = null;
			$this->lastMissionLegRelatedByFromAirportIdCriteria = null;

			$this->collMissionLegsRelatedByToAirportId = null;
			$this->lastMissionLegRelatedByToAirportIdCriteria = null;

			$this->collPilots = null;
			$this->lastPilotCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseAirport:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AirportPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			AirportPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseAirport:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseAirport:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AirportPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseAirport:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			AirportPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aWing !== null) {
				if ($this->aWing->isModified() || $this->aWing->isNew()) {
					$affectedRows += $this->aWing->save($con);
				}
				$this->setWing($this->aWing);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = AirportPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = AirportPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += AirportPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCamps !== null) {
				foreach ($this->collCamps as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFbos !== null) {
				foreach ($this->collFbos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMissionLegsRelatedByFromAirportId !== null) {
				foreach ($this->collMissionLegsRelatedByFromAirportId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMissionLegsRelatedByToAirportId !== null) {
				foreach ($this->collMissionLegsRelatedByToAirportId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPilots !== null) {
				foreach ($this->collPilots as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aWing !== null) {
				if (!$this->aWing->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aWing->getValidationFailures());
				}
			}


			if (($retval = AirportPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCamps !== null) {
					foreach ($this->collCamps as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFbos !== null) {
					foreach ($this->collFbos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMissionLegsRelatedByFromAirportId !== null) {
					foreach ($this->collMissionLegsRelatedByFromAirportId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMissionLegsRelatedByToAirportId !== null) {
					foreach ($this->collMissionLegsRelatedByToAirportId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPilots !== null) {
					foreach ($this->collPilots as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AirportPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getIdent();
				break;
			case 2:
				return $this->getName();
				break;
			case 3:
				return $this->getCity();
				break;
			case 4:
				return $this->getState();
				break;
			case 5:
				return $this->getLatitude();
				break;
			case 6:
				return $this->getLongitude();
				break;
			case 7:
				return $this->getRunwayLength();
				break;
			case 8:
				return $this->getWingId();
				break;
			case 9:
				return $this->getGmtOffset();
				break;
			case 10:
				return $this->getDstOffset();
				break;
			case 11:
				return $this->getZipcode();
				break;
			case 12:
				return $this->getClosed();
				break;
			case 13:
				return $this->getFaaSiteNumber();
				break;
			case 14:
				return $this->getAtct();
				break;
			case 15:
				return $this->getIsPrivate();
				break;
			case 16:
				return $this->getNonCommercialLandingFee();
				break;
			case 17:
				return $this->getElevation();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = AirportPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getIdent(),
			$keys[2] => $this->getName(),
			$keys[3] => $this->getCity(),
			$keys[4] => $this->getState(),
			$keys[5] => $this->getLatitude(),
			$keys[6] => $this->getLongitude(),
			$keys[7] => $this->getRunwayLength(),
			$keys[8] => $this->getWingId(),
			$keys[9] => $this->getGmtOffset(),
			$keys[10] => $this->getDstOffset(),
			$keys[11] => $this->getZipcode(),
			$keys[12] => $this->getClosed(),
			$keys[13] => $this->getFaaSiteNumber(),
			$keys[14] => $this->getAtct(),
			$keys[15] => $this->getIsPrivate(),
			$keys[16] => $this->getNonCommercialLandingFee(),
			$keys[17] => $this->getElevation(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AirportPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setIdent($value);
				break;
			case 2:
				$this->setName($value);
				break;
			case 3:
				$this->setCity($value);
				break;
			case 4:
				$this->setState($value);
				break;
			case 5:
				$this->setLatitude($value);
				break;
			case 6:
				$this->setLongitude($value);
				break;
			case 7:
				$this->setRunwayLength($value);
				break;
			case 8:
				$this->setWingId($value);
				break;
			case 9:
				$this->setGmtOffset($value);
				break;
			case 10:
				$this->setDstOffset($value);
				break;
			case 11:
				$this->setZipcode($value);
				break;
			case 12:
				$this->setClosed($value);
				break;
			case 13:
				$this->setFaaSiteNumber($value);
				break;
			case 14:
				$this->setAtct($value);
				break;
			case 15:
				$this->setIsPrivate($value);
				break;
			case 16:
				$this->setNonCommercialLandingFee($value);
				break;
			case 17:
				$this->setElevation($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = AirportPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdent($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setName($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCity($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setState($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setLatitude($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setLongitude($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setRunwayLength($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setWingId($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setGmtOffset($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setDstOffset($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setZipcode($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setClosed($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setFaaSiteNumber($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setAtct($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setIsPrivate($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setNonCommercialLandingFee($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setElevation($arr[$keys[17]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(AirportPeer::DATABASE_NAME);

		if ($this->isColumnModified(AirportPeer::ID)) $criteria->add(AirportPeer::ID, $this->id);
		if ($this->isColumnModified(AirportPeer::IDENT)) $criteria->add(AirportPeer::IDENT, $this->ident);
		if ($this->isColumnModified(AirportPeer::NAME)) $criteria->add(AirportPeer::NAME, $this->name);
		if ($this->isColumnModified(AirportPeer::CITY)) $criteria->add(AirportPeer::CITY, $this->city);
		if ($this->isColumnModified(AirportPeer::STATE)) $criteria->add(AirportPeer::STATE, $this->state);
		if ($this->isColumnModified(AirportPeer::LATITUDE)) $criteria->add(AirportPeer::LATITUDE, $this->latitude);
		if ($this->isColumnModified(AirportPeer::LONGITUDE)) $criteria->add(AirportPeer::LONGITUDE, $this->longitude);
		if ($this->isColumnModified(AirportPeer::RUNWAY_LENGTH)) $criteria->add(AirportPeer::RUNWAY_LENGTH, $this->runway_length);
		if ($this->isColumnModified(AirportPeer::WING_ID)) $criteria->add(AirportPeer::WING_ID, $this->wing_id);
		if ($this->isColumnModified(AirportPeer::GMT_OFFSET)) $criteria->add(AirportPeer::GMT_OFFSET, $this->gmt_offset);
		if ($this->isColumnModified(AirportPeer::DST_OFFSET)) $criteria->add(AirportPeer::DST_OFFSET, $this->dst_offset);
		if ($this->isColumnModified(AirportPeer::ZIPCODE)) $criteria->add(AirportPeer::ZIPCODE, $this->zipcode);
		if ($this->isColumnModified(AirportPeer::CLOSED)) $criteria->add(AirportPeer::CLOSED, $this->closed);
		if ($this->isColumnModified(AirportPeer::FAA_SITE_NUMBER)) $criteria->add(AirportPeer::FAA_SITE_NUMBER, $this->faa_site_number);
		if ($this->isColumnModified(AirportPeer::ATCT)) $criteria->add(AirportPeer::ATCT, $this->atct);
		if ($this->isColumnModified(AirportPeer::IS_PRIVATE)) $criteria->add(AirportPeer::IS_PRIVATE, $this->is_private);
		if ($this->isColumnModified(AirportPeer::NON_COMMERCIAL_LANDING_FEE)) $criteria->add(AirportPeer::NON_COMMERCIAL_LANDING_FEE, $this->non_commercial_landing_fee);
		if ($this->isColumnModified(AirportPeer::ELEVATION)) $criteria->add(AirportPeer::ELEVATION, $this->elevation);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(AirportPeer::DATABASE_NAME);

		$criteria->add(AirportPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Airport (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdent($this->ident);

		$copyObj->setName($this->name);

		$copyObj->setCity($this->city);

		$copyObj->setState($this->state);

		$copyObj->setLatitude($this->latitude);

		$copyObj->setLongitude($this->longitude);

		$copyObj->setRunwayLength($this->runway_length);

		$copyObj->setWingId($this->wing_id);

		$copyObj->setGmtOffset($this->gmt_offset);

		$copyObj->setDstOffset($this->dst_offset);

		$copyObj->setZipcode($this->zipcode);

		$copyObj->setClosed($this->closed);

		$copyObj->setFaaSiteNumber($this->faa_site_number);

		$copyObj->setAtct($this->atct);

		$copyObj->setIsPrivate($this->is_private);

		$copyObj->setNonCommercialLandingFee($this->non_commercial_landing_fee);

		$copyObj->setElevation($this->elevation);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCamps() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCamp($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFbos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFbo($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMissionLegsRelatedByFromAirportId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMissionLegRelatedByFromAirportId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMissionLegsRelatedByToAirportId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMissionLegRelatedByToAirportId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPilots() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPilot($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Airport Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     AirportPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new AirportPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Wing object.
	 *
	 * @param      Wing $v
	 * @return     Airport The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setWing(Wing $v = null)
	{
		if ($v === null) {
			$this->setWingId(NULL);
		} else {
			$this->setWingId($v->getId());
		}

		$this->aWing = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Wing object, it will not be re-added.
		if ($v !== null) {
			$v->addAirport($this);
		}

		return $this;
	}


	/**
	 * Get the associated Wing object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Wing The associated Wing object.
	 * @throws     PropelException
	 */
	public function getWing(PropelPDO $con = null)
	{
		if ($this->aWing === null && ($this->wing_id !== null)) {
			$c = new Criteria(WingPeer::DATABASE_NAME);
			$c->add(WingPeer::ID, $this->wing_id);
			$this->aWing = WingPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aWing->addAirports($this);
			 */
		}
		return $this->aWing;
	}

	/**
	 * Clears out the collCamps collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCamps()
	 */
	public function clearCamps()
	{
		$this->collCamps = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCamps collection (array).
	 *
	 * By default this just sets the collCamps collection to an empty array (like clearcollCamps());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCamps()
	{
		$this->collCamps = array();
	}

	/**
	 * Gets an array of Camp objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Airport has previously been saved, it will retrieve
	 * related Camps from storage. If this Airport is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Camp[]
	 * @throws     PropelException
	 */
	public function getCamps($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCamps === null) {
			if ($this->isNew()) {
			   $this->collCamps = array();
			} else {

				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				CampPeer::addSelectColumns($criteria);
				$this->collCamps = CampPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				CampPeer::addSelectColumns($criteria);
				if (!isset($this->lastCampCriteria) || !$this->lastCampCriteria->equals($criteria)) {
					$this->collCamps = CampPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCampCriteria = $criteria;
		return $this->collCamps;
	}

	/**
	 * Returns the number of related Camp objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Camp objects.
	 * @throws     PropelException
	 */
	public function countCamps(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCamps === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				$count = CampPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				if (!isset($this->lastCampCriteria) || !$this->lastCampCriteria->equals($criteria)) {
					$count = CampPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCamps);
				}
			} else {
				$count = count($this->collCamps);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Camp object to this object
	 * through the Camp foreign key attribute.
	 *
	 * @param      Camp $l Camp
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCamp(Camp $l)
	{
		if ($this->collCamps === null) {
			$this->initCamps();
		}
		if (!in_array($l, $this->collCamps, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCamps, $l);
			$l->setAirport($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related Camps from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getCampsJoinAgency($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCamps === null) {
			if ($this->isNew()) {
				$this->collCamps = array();
			} else {

				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				$this->collCamps = CampPeer::doSelectJoinAgency($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CampPeer::AIRPORT_ID, $this->id);

			if (!isset($this->lastCampCriteria) || !$this->lastCampCriteria->equals($criteria)) {
				$this->collCamps = CampPeer::doSelectJoinAgency($criteria, $con, $join_behavior);
			}
		}
		$this->lastCampCriteria = $criteria;

		return $this->collCamps;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related Camps from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getCampsJoinFbo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCamps === null) {
			if ($this->isNew()) {
				$this->collCamps = array();
			} else {

				$criteria->add(CampPeer::AIRPORT_ID, $this->id);

				$this->collCamps = CampPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CampPeer::AIRPORT_ID, $this->id);

			if (!isset($this->lastCampCriteria) || !$this->lastCampCriteria->equals($criteria)) {
				$this->collCamps = CampPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		}
		$this->lastCampCriteria = $criteria;

		return $this->collCamps;
	}

	/**
	 * Clears out the collFbos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFbos()
	 */
	public function clearFbos()
	{
		$this->collFbos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFbos collection (array).
	 *
	 * By default this just sets the collFbos collection to an empty array (like clearcollFbos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFbos()
	{
		$this->collFbos = array();
	}

	/**
	 * Gets an array of Fbo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Airport has previously been saved, it will retrieve
	 * related Fbos from storage. If this Airport is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Fbo[]
	 * @throws     PropelException
	 */
	public function getFbos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFbos === null) {
			if ($this->isNew()) {
			   $this->collFbos = array();
			} else {

				$criteria->add(FboPeer::AIRPORT_ID, $this->id);

				FboPeer::addSelectColumns($criteria);
				$this->collFbos = FboPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FboPeer::AIRPORT_ID, $this->id);

				FboPeer::addSelectColumns($criteria);
				if (!isset($this->lastFboCriteria) || !$this->lastFboCriteria->equals($criteria)) {
					$this->collFbos = FboPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFboCriteria = $criteria;
		return $this->collFbos;
	}

	/**
	 * Returns the number of related Fbo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Fbo objects.
	 * @throws     PropelException
	 */
	public function countFbos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFbos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FboPeer::AIRPORT_ID, $this->id);

				$count = FboPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FboPeer::AIRPORT_ID, $this->id);

				if (!isset($this->lastFboCriteria) || !$this->lastFboCriteria->equals($criteria)) {
					$count = FboPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collFbos);
				}
			} else {
				$count = count($this->collFbos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Fbo object to this object
	 * through the Fbo foreign key attribute.
	 *
	 * @param      Fbo $l Fbo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFbo(Fbo $l)
	{
		if ($this->collFbos === null) {
			$this->initFbos();
		}
		if (!in_array($l, $this->collFbos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFbos, $l);
			$l->setAirport($this);
		}
	}

	/**
	 * Clears out the collMissionLegsRelatedByFromAirportId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMissionLegsRelatedByFromAirportId()
	 */
	public function clearMissionLegsRelatedByFromAirportId()
	{
		$this->collMissionLegsRelatedByFromAirportId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMissionLegsRelatedByFromAirportId collection (array).
	 *
	 * By default this just sets the collMissionLegsRelatedByFromAirportId collection to an empty array (like clearcollMissionLegsRelatedByFromAirportId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMissionLegsRelatedByFromAirportId()
	{
		$this->collMissionLegsRelatedByFromAirportId = array();
	}

	/**
	 * Gets an array of MissionLeg objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Airport has previously been saved, it will retrieve
	 * related MissionLegsRelatedByFromAirportId from storage. If this Airport is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MissionLeg[]
	 * @throws     PropelException
	 */
	public function getMissionLegsRelatedByFromAirportId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
			   $this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				MissionLegPeer::addSelectColumns($criteria);
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				MissionLegPeer::addSelectColumns($criteria);
				if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
					$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;
		return $this->collMissionLegsRelatedByFromAirportId;
	}

	/**
	 * Returns the number of related MissionLeg objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MissionLeg objects.
	 * @throws     PropelException
	 */
	public function countMissionLegsRelatedByFromAirportId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$count = MissionLegPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
					$count = MissionLegPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMissionLegsRelatedByFromAirportId);
				}
			} else {
				$count = count($this->collMissionLegsRelatedByFromAirportId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MissionLeg object to this object
	 * through the MissionLeg foreign key attribute.
	 *
	 * @param      MissionLeg $l MissionLeg
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMissionLegRelatedByFromAirportId(MissionLeg $l)
	{
		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			$this->initMissionLegsRelatedByFromAirportId();
		}
		if (!in_array($l, $this->collMissionLegsRelatedByFromAirportId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMissionLegsRelatedByFromAirportId, $l);
			$l->setAirportRelatedByFromAirportId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinMission($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMission($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMission($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinCoordinator($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinPilotRelatedByPilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotRelatedByPilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotRelatedByPilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinMemberRelatedByCopilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMemberRelatedByCopilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMemberRelatedByCopilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinPilotRelatedByBackupPilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotRelatedByBackupPilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotRelatedByBackupPilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinMemberRelatedByBackupCopilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMemberRelatedByBackupCopilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMemberRelatedByBackupCopilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinMissionReport($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMissionReport($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinMissionReport($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinPilotAircraft($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotAircraft($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinPilotAircraft($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByFromAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByFromAirportIdJoinFbo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByFromAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByFromAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::FROM_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByFromAirportIdCriteria) || !$this->lastMissionLegRelatedByFromAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByFromAirportId = MissionLegPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByFromAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByFromAirportId;
	}

	/**
	 * Clears out the collMissionLegsRelatedByToAirportId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMissionLegsRelatedByToAirportId()
	 */
	public function clearMissionLegsRelatedByToAirportId()
	{
		$this->collMissionLegsRelatedByToAirportId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMissionLegsRelatedByToAirportId collection (array).
	 *
	 * By default this just sets the collMissionLegsRelatedByToAirportId collection to an empty array (like clearcollMissionLegsRelatedByToAirportId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMissionLegsRelatedByToAirportId()
	{
		$this->collMissionLegsRelatedByToAirportId = array();
	}

	/**
	 * Gets an array of MissionLeg objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Airport has previously been saved, it will retrieve
	 * related MissionLegsRelatedByToAirportId from storage. If this Airport is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MissionLeg[]
	 * @throws     PropelException
	 */
	public function getMissionLegsRelatedByToAirportId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
			   $this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				MissionLegPeer::addSelectColumns($criteria);
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				MissionLegPeer::addSelectColumns($criteria);
				if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
					$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;
		return $this->collMissionLegsRelatedByToAirportId;
	}

	/**
	 * Returns the number of related MissionLeg objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MissionLeg objects.
	 * @throws     PropelException
	 */
	public function countMissionLegsRelatedByToAirportId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$count = MissionLegPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
					$count = MissionLegPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMissionLegsRelatedByToAirportId);
				}
			} else {
				$count = count($this->collMissionLegsRelatedByToAirportId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MissionLeg object to this object
	 * through the MissionLeg foreign key attribute.
	 *
	 * @param      MissionLeg $l MissionLeg
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMissionLegRelatedByToAirportId(MissionLeg $l)
	{
		if ($this->collMissionLegsRelatedByToAirportId === null) {
			$this->initMissionLegsRelatedByToAirportId();
		}
		if (!in_array($l, $this->collMissionLegsRelatedByToAirportId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMissionLegsRelatedByToAirportId, $l);
			$l->setAirportRelatedByToAirportId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinMission($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMission($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMission($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinCoordinator($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinPilotRelatedByPilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotRelatedByPilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotRelatedByPilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinMemberRelatedByCopilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMemberRelatedByCopilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMemberRelatedByCopilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinPilotRelatedByBackupPilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotRelatedByBackupPilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotRelatedByBackupPilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinMemberRelatedByBackupCopilotId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMemberRelatedByBackupCopilotId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMemberRelatedByBackupCopilotId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinMissionReport($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMissionReport($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinMissionReport($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinPilotAircraft($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotAircraft($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinPilotAircraft($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related MissionLegsRelatedByToAirportId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getMissionLegsRelatedByToAirportIdJoinFbo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissionLegsRelatedByToAirportId === null) {
			if ($this->isNew()) {
				$this->collMissionLegsRelatedByToAirportId = array();
			} else {

				$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionLegPeer::TO_AIRPORT_ID, $this->id);

			if (!isset($this->lastMissionLegRelatedByToAirportIdCriteria) || !$this->lastMissionLegRelatedByToAirportIdCriteria->equals($criteria)) {
				$this->collMissionLegsRelatedByToAirportId = MissionLegPeer::doSelectJoinFbo($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionLegRelatedByToAirportIdCriteria = $criteria;

		return $this->collMissionLegsRelatedByToAirportId;
	}

	/**
	 * Clears out the collPilots collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPilots()
	 */
	public function clearPilots()
	{
		$this->collPilots = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPilots collection (array).
	 *
	 * By default this just sets the collPilots collection to an empty array (like clearcollPilots());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPilots()
	{
		$this->collPilots = array();
	}

	/**
	 * Gets an array of Pilot objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Airport has previously been saved, it will retrieve
	 * related Pilots from storage. If this Airport is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Pilot[]
	 * @throws     PropelException
	 */
	public function getPilots($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilots === null) {
			if ($this->isNew()) {
			   $this->collPilots = array();
			} else {

				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				PilotPeer::addSelectColumns($criteria);
				$this->collPilots = PilotPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				PilotPeer::addSelectColumns($criteria);
				if (!isset($this->lastPilotCriteria) || !$this->lastPilotCriteria->equals($criteria)) {
					$this->collPilots = PilotPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPilotCriteria = $criteria;
		return $this->collPilots;
	}

	/**
	 * Returns the number of related Pilot objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Pilot objects.
	 * @throws     PropelException
	 */
	public function countPilots(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPilots === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				$count = PilotPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				if (!isset($this->lastPilotCriteria) || !$this->lastPilotCriteria->equals($criteria)) {
					$count = PilotPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collPilots);
				}
			} else {
				$count = count($this->collPilots);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Pilot object to this object
	 * through the Pilot foreign key attribute.
	 *
	 * @param      Pilot $l Pilot
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPilot(Pilot $l)
	{
		if ($this->collPilots === null) {
			$this->initPilots();
		}
		if (!in_array($l, $this->collPilots, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPilots, $l);
			$l->setAirport($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related Pilots from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getPilotsJoinMember($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilots === null) {
			if ($this->isNew()) {
				$this->collPilots = array();
			} else {

				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				$this->collPilots = PilotPeer::doSelectJoinMember($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

			if (!isset($this->lastPilotCriteria) || !$this->lastPilotCriteria->equals($criteria)) {
				$this->collPilots = PilotPeer::doSelectJoinMember($criteria, $con, $join_behavior);
			}
		}
		$this->lastPilotCriteria = $criteria;

		return $this->collPilots;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related Pilots from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getPilotsJoinPilotRelatedByOrientedMemberId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilots === null) {
			if ($this->isNew()) {
				$this->collPilots = array();
			} else {

				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				$this->collPilots = PilotPeer::doSelectJoinPilotRelatedByOrientedMemberId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

			if (!isset($this->lastPilotCriteria) || !$this->lastPilotCriteria->equals($criteria)) {
				$this->collPilots = PilotPeer::doSelectJoinPilotRelatedByOrientedMemberId($criteria, $con, $join_behavior);
			}
		}
		$this->lastPilotCriteria = $criteria;

		return $this->collPilots;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Airport is new, it will return
	 * an empty collection; or if this Airport has previously
	 * been saved, it will retrieve related Pilots from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Airport.
	 */
	public function getPilotsJoinPilotRelatedByMopOrientedMemberId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AirportPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilots === null) {
			if ($this->isNew()) {
				$this->collPilots = array();
			} else {

				$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

				$this->collPilots = PilotPeer::doSelectJoinPilotRelatedByMopOrientedMemberId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PilotPeer::PRIMARY_AIRPORT_ID, $this->id);

			if (!isset($this->lastPilotCriteria) || !$this->lastPilotCriteria->equals($criteria)) {
				$this->collPilots = PilotPeer::doSelectJoinPilotRelatedByMopOrientedMemberId($criteria, $con, $join_behavior);
			}
		}
		$this->lastPilotCriteria = $criteria;

		return $this->collPilots;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCamps) {
				foreach ((array) $this->collCamps as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFbos) {
				foreach ((array) $this->collFbos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMissionLegsRelatedByFromAirportId) {
				foreach ((array) $this->collMissionLegsRelatedByFromAirportId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMissionLegsRelatedByToAirportId) {
				foreach ((array) $this->collMissionLegsRelatedByToAirportId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPilots) {
				foreach ((array) $this->collPilots as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCamps = null;
		$this->collFbos = null;
		$this->collMissionLegsRelatedByFromAirportId = null;
		$this->collMissionLegsRelatedByToAirportId = null;
		$this->collPilots = null;
			$this->aWing = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseAirport:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseAirport::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseAirport
