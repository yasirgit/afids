<?php

/**
 * Base class that represents a row from the 'camp' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Tue May 24 06:33:25 2011
 *
 * @package    lib.model.om
 */
abstract class BaseCamp extends BaseObject  implements Persistent {


  const PEER = 'CampPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CampPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the agency_id field.
	 * @var        int
	 */
	protected $agency_id;

	/**
	 * The value for the airport_id field.
	 * @var        int
	 */
	protected $airport_id;

	/**
	 * The value for the fbo_id field.
	 * @var        int
	 */
	protected $fbo_id;

	/**
	 * The value for the camp_name field.
	 * @var        string
	 */
	protected $camp_name;

	/**
	 * The value for the camp_phone field.
	 * @var        string
	 */
	protected $camp_phone;

	/**
	 * The value for the session field.
	 * @var        string
	 */
	protected $session;

	/**
	 * The value for the camp_phone_comment field.
	 * @var        string
	 */
	protected $camp_phone_comment;

	/**
	 * The value for the lodging_name field.
	 * @var        string
	 */
	protected $lodging_name;

	/**
	 * The value for the lodging_phone field.
	 * @var        string
	 */
	protected $lodging_phone;

	/**
	 * The value for the lodging_phone_comment field.
	 * @var        string
	 */
	protected $lodging_phone_comment;

	/**
	 * The value for the arrival_date field.
	 * @var        string
	 */
	protected $arrival_date;

	/**
	 * The value for the arrival_comment field.
	 * @var        string
	 */
	protected $arrival_comment;

	/**
	 * The value for the departure_date field.
	 * @var        string
	 */
	protected $departure_date;

	/**
	 * The value for the departure_comment field.
	 * @var        string
	 */
	protected $departure_comment;

	/**
	 * The value for the comment field.
	 * @var        string
	 */
	protected $comment;

	/**
	 * The value for the grouped_mission field.
	 * @var        string
	 */
	protected $grouped_mission;

	/**
	 * The value for the flight_information field.
	 * @var        string
	 */
	protected $flight_information;

	/**
	 * @var        Agency
	 */
	protected $aAgency;

	/**
	 * @var        Airport
	 */
	protected $aAirport;

	/**
	 * @var        Fbo
	 */
	protected $aFbo;

	/**
	 * @var        array CampPassenger[] Collection to store aggregation of CampPassenger objects.
	 */
	protected $collCampPassengers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCampPassengers.
	 */
	private $lastCampPassengerCriteria = null;

	/**
	 * @var        array Mission[] Collection to store aggregation of Mission objects.
	 */
	protected $collMissions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMissions.
	 */
	private $lastMissionCriteria = null;

	/**
	 * @var        array PilotRequest[] Collection to store aggregation of PilotRequest objects.
	 */
	protected $collPilotRequests;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPilotRequests.
	 */
	private $lastPilotRequestCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCamp object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [agency_id] column value.
	 * 
	 * @return     int
	 */
	public function getAgencyId()
	{
		return $this->agency_id;
	}

	/**
	 * Get the [airport_id] column value.
	 * 
	 * @return     int
	 */
	public function getAirportId()
	{
		return $this->airport_id;
	}

	/**
	 * Get the [fbo_id] column value.
	 * 
	 * @return     int
	 */
	public function getFboId()
	{
		return $this->fbo_id;
	}

	/**
	 * Get the [camp_name] column value.
	 * 
	 * @return     string
	 */
	public function getCampName()
	{
		return $this->camp_name;
	}

	/**
	 * Get the [camp_phone] column value.
	 * 
	 * @return     string
	 */
	public function getCampPhone()
	{
		return $this->camp_phone;
	}

	/**
	 * Get the [session] column value.
	 * 
	 * @return     string
	 */
	public function getSession()
	{
		return $this->session;
	}

	/**
	 * Get the [camp_phone_comment] column value.
	 * 
	 * @return     string
	 */
	public function getCampPhoneComment()
	{
		return $this->camp_phone_comment;
	}

	/**
	 * Get the [lodging_name] column value.
	 * 
	 * @return     string
	 */
	public function getLodgingName()
	{
		return $this->lodging_name;
	}

	/**
	 * Get the [lodging_phone] column value.
	 * 
	 * @return     string
	 */
	public function getLodgingPhone()
	{
		return $this->lodging_phone;
	}

	/**
	 * Get the [lodging_phone_comment] column value.
	 * 
	 * @return     string
	 */
	public function getLodgingPhoneComment()
	{
		return $this->lodging_phone_comment;
	}

	/**
	 * Get the [optionally formatted] temporal [arrival_date] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getArrivalDate($format = 'Y-m-d H:i:s')
	{
		if ($this->arrival_date === null) {
			return null;
		}


		if ($this->arrival_date === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->arrival_date);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->arrival_date, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [arrival_comment] column value.
	 * 
	 * @return     string
	 */
	public function getArrivalComment()
	{
		return $this->arrival_comment;
	}

	/**
	 * Get the [optionally formatted] temporal [departure_date] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDepartureDate($format = 'Y-m-d H:i:s')
	{
		if ($this->departure_date === null) {
			return null;
		}


		if ($this->departure_date === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->departure_date);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->departure_date, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [departure_comment] column value.
	 * 
	 * @return     string
	 */
	public function getDepartureComment()
	{
		return $this->departure_comment;
	}

	/**
	 * Get the [comment] column value.
	 * 
	 * @return     string
	 */
	public function getComment()
	{
		return $this->comment;
	}

	/**
	 * Get the [grouped_mission] column value.
	 * 
	 * @return     string
	 */
	public function getGroupedMission()
	{
		return $this->grouped_mission;
	}

	/**
	 * Get the [flight_information] column value.
	 * 
	 * @return     string
	 */
	public function getFlightInformation()
	{
		return $this->flight_information;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CampPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [agency_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setAgencyId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->agency_id !== $v) {
			$this->agency_id = $v;
			$this->modifiedColumns[] = CampPeer::AGENCY_ID;
		}

		if ($this->aAgency !== null && $this->aAgency->getId() !== $v) {
			$this->aAgency = null;
		}

		return $this;
	} // setAgencyId()

	/**
	 * Set the value of [airport_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setAirportId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->airport_id !== $v) {
			$this->airport_id = $v;
			$this->modifiedColumns[] = CampPeer::AIRPORT_ID;
		}

		if ($this->aAirport !== null && $this->aAirport->getId() !== $v) {
			$this->aAirport = null;
		}

		return $this;
	} // setAirportId()

	/**
	 * Set the value of [fbo_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setFboId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->fbo_id !== $v) {
			$this->fbo_id = $v;
			$this->modifiedColumns[] = CampPeer::FBO_ID;
		}

		if ($this->aFbo !== null && $this->aFbo->getId() !== $v) {
			$this->aFbo = null;
		}

		return $this;
	} // setFboId()

	/**
	 * Set the value of [camp_name] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setCampName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->camp_name !== $v) {
			$this->camp_name = $v;
			$this->modifiedColumns[] = CampPeer::CAMP_NAME;
		}

		return $this;
	} // setCampName()

	/**
	 * Set the value of [camp_phone] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setCampPhone($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->camp_phone !== $v) {
			$this->camp_phone = $v;
			$this->modifiedColumns[] = CampPeer::CAMP_PHONE;
		}

		return $this;
	} // setCampPhone()

	/**
	 * Set the value of [session] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setSession($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->session !== $v) {
			$this->session = $v;
			$this->modifiedColumns[] = CampPeer::SESSION;
		}

		return $this;
	} // setSession()

	/**
	 * Set the value of [camp_phone_comment] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setCampPhoneComment($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->camp_phone_comment !== $v) {
			$this->camp_phone_comment = $v;
			$this->modifiedColumns[] = CampPeer::CAMP_PHONE_COMMENT;
		}

		return $this;
	} // setCampPhoneComment()

	/**
	 * Set the value of [lodging_name] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setLodgingName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->lodging_name !== $v) {
			$this->lodging_name = $v;
			$this->modifiedColumns[] = CampPeer::LODGING_NAME;
		}

		return $this;
	} // setLodgingName()

	/**
	 * Set the value of [lodging_phone] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setLodgingPhone($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->lodging_phone !== $v) {
			$this->lodging_phone = $v;
			$this->modifiedColumns[] = CampPeer::LODGING_PHONE;
		}

		return $this;
	} // setLodgingPhone()

	/**
	 * Set the value of [lodging_phone_comment] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setLodgingPhoneComment($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->lodging_phone_comment !== $v) {
			$this->lodging_phone_comment = $v;
			$this->modifiedColumns[] = CampPeer::LODGING_PHONE_COMMENT;
		}

		return $this;
	} // setLodgingPhoneComment()

	/**
	 * Sets the value of [arrival_date] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setArrivalDate($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->arrival_date !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->arrival_date !== null && $tmpDt = new DateTime($this->arrival_date)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->arrival_date = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CampPeer::ARRIVAL_DATE;
			}
		} // if either are not null

		return $this;
	} // setArrivalDate()

	/**
	 * Set the value of [arrival_comment] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setArrivalComment($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->arrival_comment !== $v) {
			$this->arrival_comment = $v;
			$this->modifiedColumns[] = CampPeer::ARRIVAL_COMMENT;
		}

		return $this;
	} // setArrivalComment()

	/**
	 * Sets the value of [departure_date] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setDepartureDate($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->departure_date !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->departure_date !== null && $tmpDt = new DateTime($this->departure_date)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->departure_date = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CampPeer::DEPARTURE_DATE;
			}
		} // if either are not null

		return $this;
	} // setDepartureDate()

	/**
	 * Set the value of [departure_comment] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setDepartureComment($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->departure_comment !== $v) {
			$this->departure_comment = $v;
			$this->modifiedColumns[] = CampPeer::DEPARTURE_COMMENT;
		}

		return $this;
	} // setDepartureComment()

	/**
	 * Set the value of [comment] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setComment($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->comment !== $v) {
			$this->comment = $v;
			$this->modifiedColumns[] = CampPeer::COMMENT;
		}

		return $this;
	} // setComment()

	/**
	 * Set the value of [grouped_mission] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setGroupedMission($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->grouped_mission !== $v) {
			$this->grouped_mission = $v;
			$this->modifiedColumns[] = CampPeer::GROUPED_MISSION;
		}

		return $this;
	} // setGroupedMission()

	/**
	 * Set the value of [flight_information] column.
	 * 
	 * @param      string $v new value
	 * @return     Camp The current object (for fluent API support)
	 */
	public function setFlightInformation($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->flight_information !== $v) {
			$this->flight_information = $v;
			$this->modifiedColumns[] = CampPeer::FLIGHT_INFORMATION;
		}

		return $this;
	} // setFlightInformation()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->agency_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->airport_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->fbo_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->camp_name = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->camp_phone = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->session = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->camp_phone_comment = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->lodging_name = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->lodging_phone = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->lodging_phone_comment = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->arrival_date = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->arrival_comment = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->departure_date = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->departure_comment = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->comment = ($row[$startcol + 15] !== null) ? (string) $row[$startcol + 15] : null;
			$this->grouped_mission = ($row[$startcol + 16] !== null) ? (string) $row[$startcol + 16] : null;
			$this->flight_information = ($row[$startcol + 17] !== null) ? (string) $row[$startcol + 17] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 18; // 18 = CampPeer::NUM_COLUMNS - CampPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Camp object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aAgency !== null && $this->agency_id !== $this->aAgency->getId()) {
			$this->aAgency = null;
		}
		if ($this->aAirport !== null && $this->airport_id !== $this->aAirport->getId()) {
			$this->aAirport = null;
		}
		if ($this->aFbo !== null && $this->fbo_id !== $this->aFbo->getId()) {
			$this->aFbo = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CampPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CampPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aAgency = null;
			$this->aAirport = null;
			$this->aFbo = null;
			$this->collCampPassengers = null;
			$this->lastCampPassengerCriteria = null;

			$this->collMissions = null;
			$this->lastMissionCriteria = null;

			$this->collPilotRequests = null;
			$this->lastPilotRequestCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCamp:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CampPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CampPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCamp:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCamp:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CampPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCamp:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CampPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAgency !== null) {
				if ($this->aAgency->isModified() || $this->aAgency->isNew()) {
					$affectedRows += $this->aAgency->save($con);
				}
				$this->setAgency($this->aAgency);
			}

			if ($this->aAirport !== null) {
				if ($this->aAirport->isModified() || $this->aAirport->isNew()) {
					$affectedRows += $this->aAirport->save($con);
				}
				$this->setAirport($this->aAirport);
			}

			if ($this->aFbo !== null) {
				if ($this->aFbo->isModified() || $this->aFbo->isNew()) {
					$affectedRows += $this->aFbo->save($con);
				}
				$this->setFbo($this->aFbo);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CampPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CampPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CampPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCampPassengers !== null) {
				foreach ($this->collCampPassengers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMissions !== null) {
				foreach ($this->collMissions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPilotRequests !== null) {
				foreach ($this->collPilotRequests as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAgency !== null) {
				if (!$this->aAgency->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAgency->getValidationFailures());
				}
			}

			if ($this->aAirport !== null) {
				if (!$this->aAirport->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAirport->getValidationFailures());
				}
			}

			if ($this->aFbo !== null) {
				if (!$this->aFbo->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFbo->getValidationFailures());
				}
			}


			if (($retval = CampPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCampPassengers !== null) {
					foreach ($this->collCampPassengers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMissions !== null) {
					foreach ($this->collMissions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPilotRequests !== null) {
					foreach ($this->collPilotRequests as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CampPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getAgencyId();
				break;
			case 2:
				return $this->getAirportId();
				break;
			case 3:
				return $this->getFboId();
				break;
			case 4:
				return $this->getCampName();
				break;
			case 5:
				return $this->getCampPhone();
				break;
			case 6:
				return $this->getSession();
				break;
			case 7:
				return $this->getCampPhoneComment();
				break;
			case 8:
				return $this->getLodgingName();
				break;
			case 9:
				return $this->getLodgingPhone();
				break;
			case 10:
				return $this->getLodgingPhoneComment();
				break;
			case 11:
				return $this->getArrivalDate();
				break;
			case 12:
				return $this->getArrivalComment();
				break;
			case 13:
				return $this->getDepartureDate();
				break;
			case 14:
				return $this->getDepartureComment();
				break;
			case 15:
				return $this->getComment();
				break;
			case 16:
				return $this->getGroupedMission();
				break;
			case 17:
				return $this->getFlightInformation();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CampPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getAgencyId(),
			$keys[2] => $this->getAirportId(),
			$keys[3] => $this->getFboId(),
			$keys[4] => $this->getCampName(),
			$keys[5] => $this->getCampPhone(),
			$keys[6] => $this->getSession(),
			$keys[7] => $this->getCampPhoneComment(),
			$keys[8] => $this->getLodgingName(),
			$keys[9] => $this->getLodgingPhone(),
			$keys[10] => $this->getLodgingPhoneComment(),
			$keys[11] => $this->getArrivalDate(),
			$keys[12] => $this->getArrivalComment(),
			$keys[13] => $this->getDepartureDate(),
			$keys[14] => $this->getDepartureComment(),
			$keys[15] => $this->getComment(),
			$keys[16] => $this->getGroupedMission(),
			$keys[17] => $this->getFlightInformation(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CampPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setAgencyId($value);
				break;
			case 2:
				$this->setAirportId($value);
				break;
			case 3:
				$this->setFboId($value);
				break;
			case 4:
				$this->setCampName($value);
				break;
			case 5:
				$this->setCampPhone($value);
				break;
			case 6:
				$this->setSession($value);
				break;
			case 7:
				$this->setCampPhoneComment($value);
				break;
			case 8:
				$this->setLodgingName($value);
				break;
			case 9:
				$this->setLodgingPhone($value);
				break;
			case 10:
				$this->setLodgingPhoneComment($value);
				break;
			case 11:
				$this->setArrivalDate($value);
				break;
			case 12:
				$this->setArrivalComment($value);
				break;
			case 13:
				$this->setDepartureDate($value);
				break;
			case 14:
				$this->setDepartureComment($value);
				break;
			case 15:
				$this->setComment($value);
				break;
			case 16:
				$this->setGroupedMission($value);
				break;
			case 17:
				$this->setFlightInformation($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CampPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setAgencyId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setAirportId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setFboId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCampName($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCampPhone($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setSession($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCampPhoneComment($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setLodgingName($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setLodgingPhone($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setLodgingPhoneComment($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setArrivalDate($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setArrivalComment($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setDepartureDate($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setDepartureComment($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setComment($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setGroupedMission($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setFlightInformation($arr[$keys[17]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CampPeer::DATABASE_NAME);

		if ($this->isColumnModified(CampPeer::ID)) $criteria->add(CampPeer::ID, $this->id);
		if ($this->isColumnModified(CampPeer::AGENCY_ID)) $criteria->add(CampPeer::AGENCY_ID, $this->agency_id);
		if ($this->isColumnModified(CampPeer::AIRPORT_ID)) $criteria->add(CampPeer::AIRPORT_ID, $this->airport_id);
		if ($this->isColumnModified(CampPeer::FBO_ID)) $criteria->add(CampPeer::FBO_ID, $this->fbo_id);
		if ($this->isColumnModified(CampPeer::CAMP_NAME)) $criteria->add(CampPeer::CAMP_NAME, $this->camp_name);
		if ($this->isColumnModified(CampPeer::CAMP_PHONE)) $criteria->add(CampPeer::CAMP_PHONE, $this->camp_phone);
		if ($this->isColumnModified(CampPeer::SESSION)) $criteria->add(CampPeer::SESSION, $this->session);
		if ($this->isColumnModified(CampPeer::CAMP_PHONE_COMMENT)) $criteria->add(CampPeer::CAMP_PHONE_COMMENT, $this->camp_phone_comment);
		if ($this->isColumnModified(CampPeer::LODGING_NAME)) $criteria->add(CampPeer::LODGING_NAME, $this->lodging_name);
		if ($this->isColumnModified(CampPeer::LODGING_PHONE)) $criteria->add(CampPeer::LODGING_PHONE, $this->lodging_phone);
		if ($this->isColumnModified(CampPeer::LODGING_PHONE_COMMENT)) $criteria->add(CampPeer::LODGING_PHONE_COMMENT, $this->lodging_phone_comment);
		if ($this->isColumnModified(CampPeer::ARRIVAL_DATE)) $criteria->add(CampPeer::ARRIVAL_DATE, $this->arrival_date);
		if ($this->isColumnModified(CampPeer::ARRIVAL_COMMENT)) $criteria->add(CampPeer::ARRIVAL_COMMENT, $this->arrival_comment);
		if ($this->isColumnModified(CampPeer::DEPARTURE_DATE)) $criteria->add(CampPeer::DEPARTURE_DATE, $this->departure_date);
		if ($this->isColumnModified(CampPeer::DEPARTURE_COMMENT)) $criteria->add(CampPeer::DEPARTURE_COMMENT, $this->departure_comment);
		if ($this->isColumnModified(CampPeer::COMMENT)) $criteria->add(CampPeer::COMMENT, $this->comment);
		if ($this->isColumnModified(CampPeer::GROUPED_MISSION)) $criteria->add(CampPeer::GROUPED_MISSION, $this->grouped_mission);
		if ($this->isColumnModified(CampPeer::FLIGHT_INFORMATION)) $criteria->add(CampPeer::FLIGHT_INFORMATION, $this->flight_information);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CampPeer::DATABASE_NAME);

		$criteria->add(CampPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Camp (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setAgencyId($this->agency_id);

		$copyObj->setAirportId($this->airport_id);

		$copyObj->setFboId($this->fbo_id);

		$copyObj->setCampName($this->camp_name);

		$copyObj->setCampPhone($this->camp_phone);

		$copyObj->setSession($this->session);

		$copyObj->setCampPhoneComment($this->camp_phone_comment);

		$copyObj->setLodgingName($this->lodging_name);

		$copyObj->setLodgingPhone($this->lodging_phone);

		$copyObj->setLodgingPhoneComment($this->lodging_phone_comment);

		$copyObj->setArrivalDate($this->arrival_date);

		$copyObj->setArrivalComment($this->arrival_comment);

		$copyObj->setDepartureDate($this->departure_date);

		$copyObj->setDepartureComment($this->departure_comment);

		$copyObj->setComment($this->comment);

		$copyObj->setGroupedMission($this->grouped_mission);

		$copyObj->setFlightInformation($this->flight_information);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCampPassengers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCampPassenger($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMissions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMission($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPilotRequests() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPilotRequest($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Camp Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CampPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CampPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Agency object.
	 *
	 * @param      Agency $v
	 * @return     Camp The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAgency(Agency $v = null)
	{
		if ($v === null) {
			$this->setAgencyId(NULL);
		} else {
			$this->setAgencyId($v->getId());
		}

		$this->aAgency = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Agency object, it will not be re-added.
		if ($v !== null) {
			$v->addCamp($this);
		}

		return $this;
	}


	/**
	 * Get the associated Agency object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Agency The associated Agency object.
	 * @throws     PropelException
	 */
	public function getAgency(PropelPDO $con = null)
	{
		if ($this->aAgency === null && ($this->agency_id !== null)) {
			$c = new Criteria(AgencyPeer::DATABASE_NAME);
			$c->add(AgencyPeer::ID, $this->agency_id);
			$this->aAgency = AgencyPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aAgency->addCamps($this);
			 */
		}
		return $this->aAgency;
	}

	/**
	 * Declares an association between this object and a Airport object.
	 *
	 * @param      Airport $v
	 * @return     Camp The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAirport(Airport $v = null)
	{
		if ($v === null) {
			$this->setAirportId(NULL);
		} else {
			$this->setAirportId($v->getId());
		}

		$this->aAirport = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Airport object, it will not be re-added.
		if ($v !== null) {
			$v->addCamp($this);
		}

		return $this;
	}


	/**
	 * Get the associated Airport object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Airport The associated Airport object.
	 * @throws     PropelException
	 */
	public function getAirport(PropelPDO $con = null)
	{
		if ($this->aAirport === null && ($this->airport_id !== null)) {
			$c = new Criteria(AirportPeer::DATABASE_NAME);
			$c->add(AirportPeer::ID, $this->airport_id);
			$this->aAirport = AirportPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aAirport->addCamps($this);
			 */
		}
		return $this->aAirport;
	}

	/**
	 * Declares an association between this object and a Fbo object.
	 *
	 * @param      Fbo $v
	 * @return     Camp The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFbo(Fbo $v = null)
	{
		if ($v === null) {
			$this->setFboId(NULL);
		} else {
			$this->setFboId($v->getId());
		}

		$this->aFbo = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Fbo object, it will not be re-added.
		if ($v !== null) {
			$v->addCamp($this);
		}

		return $this;
	}


	/**
	 * Get the associated Fbo object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Fbo The associated Fbo object.
	 * @throws     PropelException
	 */
	public function getFbo(PropelPDO $con = null)
	{
		if ($this->aFbo === null && ($this->fbo_id !== null)) {
			$c = new Criteria(FboPeer::DATABASE_NAME);
			$c->add(FboPeer::ID, $this->fbo_id);
			$this->aFbo = FboPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aFbo->addCamps($this);
			 */
		}
		return $this->aFbo;
	}

	/**
	 * Clears out the collCampPassengers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCampPassengers()
	 */
	public function clearCampPassengers()
	{
		$this->collCampPassengers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCampPassengers collection (array).
	 *
	 * By default this just sets the collCampPassengers collection to an empty array (like clearcollCampPassengers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCampPassengers()
	{
		$this->collCampPassengers = array();
	}

	/**
	 * Gets an array of CampPassenger objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Camp has previously been saved, it will retrieve
	 * related CampPassengers from storage. If this Camp is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CampPassenger[]
	 * @throws     PropelException
	 */
	public function getCampPassengers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCampPassengers === null) {
			if ($this->isNew()) {
			   $this->collCampPassengers = array();
			} else {

				$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

				CampPassengerPeer::addSelectColumns($criteria);
				$this->collCampPassengers = CampPassengerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

				CampPassengerPeer::addSelectColumns($criteria);
				if (!isset($this->lastCampPassengerCriteria) || !$this->lastCampPassengerCriteria->equals($criteria)) {
					$this->collCampPassengers = CampPassengerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCampPassengerCriteria = $criteria;
		return $this->collCampPassengers;
	}

	/**
	 * Returns the number of related CampPassenger objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CampPassenger objects.
	 * @throws     PropelException
	 */
	public function countCampPassengers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCampPassengers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

				$count = CampPassengerPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

				if (!isset($this->lastCampPassengerCriteria) || !$this->lastCampPassengerCriteria->equals($criteria)) {
					$count = CampPassengerPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCampPassengers);
				}
			} else {
				$count = count($this->collCampPassengers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CampPassenger object to this object
	 * through the CampPassenger foreign key attribute.
	 *
	 * @param      CampPassenger $l CampPassenger
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCampPassenger(CampPassenger $l)
	{
		if ($this->collCampPassengers === null) {
			$this->initCampPassengers();
		}
		if (!in_array($l, $this->collCampPassengers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCampPassengers, $l);
			$l->setCamp($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related CampPassengers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getCampPassengersJoinPassenger($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCampPassengers === null) {
			if ($this->isNew()) {
				$this->collCampPassengers = array();
			} else {

				$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

				$this->collCampPassengers = CampPassengerPeer::doSelectJoinPassenger($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CampPassengerPeer::CAMP_ID, $this->id);

			if (!isset($this->lastCampPassengerCriteria) || !$this->lastCampPassengerCriteria->equals($criteria)) {
				$this->collCampPassengers = CampPassengerPeer::doSelectJoinPassenger($criteria, $con, $join_behavior);
			}
		}
		$this->lastCampPassengerCriteria = $criteria;

		return $this->collCampPassengers;
	}

	/**
	 * Clears out the collMissions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMissions()
	 */
	public function clearMissions()
	{
		$this->collMissions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMissions collection (array).
	 *
	 * By default this just sets the collMissions collection to an empty array (like clearcollMissions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMissions()
	{
		$this->collMissions = array();
	}

	/**
	 * Gets an array of Mission objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Camp has previously been saved, it will retrieve
	 * related Missions from storage. If this Camp is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Mission[]
	 * @throws     PropelException
	 */
	public function getMissions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
			   $this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				MissionPeer::addSelectColumns($criteria);
				$this->collMissions = MissionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				MissionPeer::addSelectColumns($criteria);
				if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
					$this->collMissions = MissionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMissionCriteria = $criteria;
		return $this->collMissions;
	}

	/**
	 * Returns the number of related Mission objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Mission objects.
	 * @throws     PropelException
	 */
	public function countMissions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$count = MissionPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
					$count = MissionPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMissions);
				}
			} else {
				$count = count($this->collMissions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Mission object to this object
	 * through the Mission foreign key attribute.
	 *
	 * @param      Mission $l Mission
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMission(Mission $l)
	{
		if ($this->collMissions === null) {
			$this->initMissions();
		}
		if (!in_array($l, $this->collMissions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMissions, $l);
			$l->setCamp($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinMissionRequest($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinMissionRequest($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinMissionRequest($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinItinerary($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinItinerary($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinItinerary($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinMissionType($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinMissionType($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinMissionType($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinPassenger($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinPassenger($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinPassenger($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinRequesterRelatedByRequesterId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinRequesterRelatedByRequesterId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinRequesterRelatedByRequesterId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinAgencyRelatedByAgencyId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinAgencyRelatedByAgencyId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinAgencyRelatedByAgencyId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinRequesterRelatedByOtherRequesterId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinRequesterRelatedByOtherRequesterId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinRequesterRelatedByOtherRequesterId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinAgencyRelatedByOtherAgencyId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinAgencyRelatedByOtherAgencyId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinAgencyRelatedByOtherAgencyId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related Missions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getMissionsJoinCoordinator($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMissions === null) {
			if ($this->isNew()) {
				$this->collMissions = array();
			} else {

				$criteria->add(MissionPeer::CAMP_ID, $this->id);

				$this->collMissions = MissionPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MissionPeer::CAMP_ID, $this->id);

			if (!isset($this->lastMissionCriteria) || !$this->lastMissionCriteria->equals($criteria)) {
				$this->collMissions = MissionPeer::doSelectJoinCoordinator($criteria, $con, $join_behavior);
			}
		}
		$this->lastMissionCriteria = $criteria;

		return $this->collMissions;
	}

	/**
	 * Clears out the collPilotRequests collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPilotRequests()
	 */
	public function clearPilotRequests()
	{
		$this->collPilotRequests = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPilotRequests collection (array).
	 *
	 * By default this just sets the collPilotRequests collection to an empty array (like clearcollPilotRequests());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPilotRequests()
	{
		$this->collPilotRequests = array();
	}

	/**
	 * Gets an array of PilotRequest objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Camp has previously been saved, it will retrieve
	 * related PilotRequests from storage. If this Camp is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array PilotRequest[]
	 * @throws     PropelException
	 */
	public function getPilotRequests($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilotRequests === null) {
			if ($this->isNew()) {
			   $this->collPilotRequests = array();
			} else {

				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				PilotRequestPeer::addSelectColumns($criteria);
				$this->collPilotRequests = PilotRequestPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				PilotRequestPeer::addSelectColumns($criteria);
				if (!isset($this->lastPilotRequestCriteria) || !$this->lastPilotRequestCriteria->equals($criteria)) {
					$this->collPilotRequests = PilotRequestPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPilotRequestCriteria = $criteria;
		return $this->collPilotRequests;
	}

	/**
	 * Returns the number of related PilotRequest objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PilotRequest objects.
	 * @throws     PropelException
	 */
	public function countPilotRequests(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPilotRequests === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				$count = PilotRequestPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				if (!isset($this->lastPilotRequestCriteria) || !$this->lastPilotRequestCriteria->equals($criteria)) {
					$count = PilotRequestPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collPilotRequests);
				}
			} else {
				$count = count($this->collPilotRequests);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a PilotRequest object to this object
	 * through the PilotRequest foreign key attribute.
	 *
	 * @param      PilotRequest $l PilotRequest
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPilotRequest(PilotRequest $l)
	{
		if ($this->collPilotRequests === null) {
			$this->initPilotRequests();
		}
		if (!in_array($l, $this->collPilotRequests, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPilotRequests, $l);
			$l->setCamp($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related PilotRequests from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getPilotRequestsJoinMember($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilotRequests === null) {
			if ($this->isNew()) {
				$this->collPilotRequests = array();
			} else {

				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				$this->collPilotRequests = PilotRequestPeer::doSelectJoinMember($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

			if (!isset($this->lastPilotRequestCriteria) || !$this->lastPilotRequestCriteria->equals($criteria)) {
				$this->collPilotRequests = PilotRequestPeer::doSelectJoinMember($criteria, $con, $join_behavior);
			}
		}
		$this->lastPilotRequestCriteria = $criteria;

		return $this->collPilotRequests;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Camp is new, it will return
	 * an empty collection; or if this Camp has previously
	 * been saved, it will retrieve related PilotRequests from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Camp.
	 */
	public function getPilotRequestsJoinMissionLeg($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CampPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPilotRequests === null) {
			if ($this->isNew()) {
				$this->collPilotRequests = array();
			} else {

				$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

				$this->collPilotRequests = PilotRequestPeer::doSelectJoinMissionLeg($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PilotRequestPeer::GROUP_CAMP_ID, $this->id);

			if (!isset($this->lastPilotRequestCriteria) || !$this->lastPilotRequestCriteria->equals($criteria)) {
				$this->collPilotRequests = PilotRequestPeer::doSelectJoinMissionLeg($criteria, $con, $join_behavior);
			}
		}
		$this->lastPilotRequestCriteria = $criteria;

		return $this->collPilotRequests;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCampPassengers) {
				foreach ((array) $this->collCampPassengers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMissions) {
				foreach ((array) $this->collMissions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPilotRequests) {
				foreach ((array) $this->collPilotRequests as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCampPassengers = null;
		$this->collMissions = null;
		$this->collPilotRequests = null;
			$this->aAgency = null;
			$this->aAirport = null;
			$this->aFbo = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCamp:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCamp::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCamp
